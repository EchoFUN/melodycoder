<!DOCTYPE html>
<html>
    <head>
        <title><%= site.SITE_NAME %></title>
         <script type="text/javascript">
        function approve(target) {
          
        }
        function reject(target) {
        
        }
        </script>
    </head>
    <body>
      <% if (hasRight) {%>
    <% if(status.code == 0) { %>
    请求失败！<br />
    原因为<%= status.content %>
    <% } else { %>
    待审核评论为：
      <ul>
      <% for(var i=0; i<comments.length; i++) { %>
        <li>       
          <div><%= comments[i].name %>&nbsp;评论于<%= comments[i].date %>&nbsp;内容为：<%= comments[i].content %>&nbsp;文章id：<%= comments[i].pid %></div>
          <div>
            <a cid="<%= comments[i]._id %>" href="javacript:;" onclick="approve(this)">批准</a>
            <a cid="<%= comments[i]._id %>" href="javacript:;" onclick="reject(this)">驳回</a>
          </div>  
        </li>    
      <% } %>
      </ul>
    <% } %>
  <% } else { %>
  对不起，您没有权限访问此页面，请登录。
  <% } %>
    </body>
</html>