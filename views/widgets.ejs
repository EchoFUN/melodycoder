 <% if(typeof links != 'undefined') { %>
<div class="widget">
    <h3>链接</h3>
    <ul>
        <% for(var j=0; j<links.length; j++) { %>
        <li><a target="_blank" href="<%= links[j].url %>" ><%= links[j].title %></a></li>
        <% } %>
    </ul>
</div>
<% } %>

<% if(typeof rectPosts != 'undefined') {%>
<div class="widget">
    <h3>近期文章</h3>
    <ul>
    	<% for(var j=0; j<rectPosts.length; j++) { %>
        <li><a target="_blank" href="<%= site.SITE_BASE_URL + '/post?pid=' + rectPosts[j]._id %>" title="<%= rectPosts[j].title %>"><%= rectPosts[j].title %></a></li>
    	<% } %>
    </ul>
</div>    
<% } %>  

<div class="widget">        
    <h3>文章归档</h3>       
    <ul>
    	<li><a href="#nogo">2012年11月</a></li>
    </ul>
</div>